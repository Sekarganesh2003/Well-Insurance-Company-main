
import ClaimUploadForm from "@/components/claims/ClaimUploadForm";

const UploadClaim = () => {
  return (
    <div className="py-6">
      <h1 className="text-3xl font-bold mb-6">Submit a New Claim</h1>
      <ClaimUploadForm />
    </div>
  );
};

export default UploadClaim;
